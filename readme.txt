instructions
run node server:
$ npm i
$ node app.js

run flask server:
install flask dependencies: keras, tensorflow, python3.6, theano, etc according to train_app.py
$ export FLASK_APP=predict_app.py
$ flask run
